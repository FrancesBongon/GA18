<?php
$conn = mysqli_connect("localhost","root","","class_db");

// Wrong: Including it within the SQL query would result to SQL injection
// $sql = "SELECT * FROM students WHERE age = $age";

// Right: Use prepared statement to be safe against SQL injection
$age = $_GET['age'];

$stmt = $conn->prepare("SELECT * FROM students WHERE age = ?");
$stmt->bind_param("i", $age); // "i" means integer
$stmt->execute();

$res = $stmt->get_result();

/*
I used prepared statement in order to prevent the user to inject the SQL. 
From the previous result, if the user inputs 1 OR 1=1, all the records would come out.
However, with this correction, I made it so that the $age would be considered as integer only.
Therefore, the query is now safe. =).
*/

?>
