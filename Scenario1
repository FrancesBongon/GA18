<?php
$conn = mysqli_connect("localhost", "root", "", "class_db");

//Wrong: wrong use of $_POST when the data is from URL (GET request)
//$id = $_POST['id'];

//Right: Get the value of id using $_GET
$id = $_GET['id'];

/*I changed the $_POST into $_GET because the data is acquired from the URL. 
Because if GET request is being used, so ?id=3 is used in the URL. It will not go through POST form.
If POST request otherwise, it will be undefined index because there is no POST data is received./*

$sql = "SELECT * FROM students WHERE id = $id";
$res = mysqli_query($conn, $sql);
$r = mysqli_fetch_assoc($res);
echo $r['first_name'];
?>
